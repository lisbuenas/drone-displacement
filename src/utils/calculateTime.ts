export async function calculateTime(path: string[]){
    await Promise.all((path.map(cell =>{
        // call 

        
    })))
}

export function calculateStep(origin:string,destination:string){

    
    return
}